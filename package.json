{
  "name": "trace-shuttle-monorepo",
  "private": true,
  "description": "Monorepo for trace-shuttle core, CLI review tooling, and ESLint integration.",
  "license": "MIT",
  "author": "kitazzz",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@trace-shuttle/eslint-plugin": "workspace:*",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20"
  }
}
