// @trace[test spec=SPEC-CHECKOUT-GIFT-WRAP]

import { describe, expect, it } from "vitest";

describe("gift wrap", () => {
  it("can be selected during checkout", () => {
    // @trace[test spec=SPEC-CHECKOUT-GIFT-WRAP]
    expect(true).toBe(true);
  });
});
